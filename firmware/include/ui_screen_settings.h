// Project  : Jarvis Edge Node
// File     : ui_screen_settings.h
// Purpose  : "Settings" tile — on-device WiFi/backend/MQTT configuration
// Depends  : ui.h

#pragma once
#include <lvgl.h>

// Builds the settings tile's contents inside `tile`.
void uiSettingsScreenInit(lv_obj_t * tile);
